[tool.poetry]
name = "aganitha"
version = "0.1.0"
description = "Fetch and process PubMed research papers with non-academic authors."
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28"

[tool.poetry.scripts]
get-papers-list = "main:main"
